# Foundry
cache/
out/
broadcast/

# Environment variables (NEVER commit)
.env
.env.local
.env*.local

# Dependencies are pinned as git submodules in the repository root.

# Build artifacts
*.abi
*.bin

# OS files
.DS_Store
